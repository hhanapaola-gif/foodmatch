# Docker Compose variables for the MONITORING server stack.
# Copy to ".env" in this same folder and fill in real secrets before deploying.

GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=change-me-strong-grafana-password
GRAFANA_ROOT_URL=https://your-domain.example/grafana
